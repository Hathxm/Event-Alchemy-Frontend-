# Disable the CRA ESLint webpack plugin during production builds.
# On CI (Vercel sets CI=true) react-scripts promotes every ESLint warning
# to a build-breaking error. This file is committed (unlike .env, which is
# gitignored) so Vercel picks it up and the build no longer fails on warnings.
DISABLE_ESLINT_PLUGIN=true
